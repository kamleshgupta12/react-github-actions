name: "first workflow"
on: 
  push:
  pull_request:
    branches:
      - main
      - develop
      - qa
    
jobs: 
    first-job: 
      runs-on: ubuntu-latest
      steps: 
        - name: checkout
          uses: actions/checkout@v4
          with:
            node-version: 24
            cache: npm

        - name: install dependency
          run: npm ci

        - name: make react build
          run: npm run build